# Bậc 3 — staging.  Bản thật nằm ở /etc/kaiju/staging.env TRÊN MÁY CHỦ, quyền
# 600, KHÔNG nằm trong repository.
#
# Khác biệt so với bậc 1–2 không phải là "nhiều biến hơn" mà là: mọi dịch vụ
# đều thật (CON-73). Không bộ bắt email, không kho lưu trữ tự dựng, không
# container quan sát gói sẵn.

# Thẻ cố định gắn với commit (CON-70). KHÔNG bao giờ dùng `latest`.
KAIJU_IMAGE=ghcr.io/OWNER/kaiju-backend:sha-REPLACE
KAIJU_FRONTEND_IMAGE=ghcr.io/OWNER/kaiju-frontend:sha-REPLACE
KAIJU_DOMAIN=staging.kaiju.example.com

# PostgreSQL cài thẳng trên máy chủ, không phải container — hiệu năng vào ra và
# độ bền dữ liệu. `host.docker.internal` hoặc địa chỉ của cầu nối Docker.
KAIJU_DB_URL=jdbc:postgresql://host.docker.internal:5432/kaiju
KAIJU_DB_LISTEN_URL=jdbc:postgresql://host.docker.internal:5432/kaiju
KAIJU_DB_USER=kaiju
KAIJU_DB_PASSWORD=REPLACE

KAIJU_REDIS_URL=redis://host.docker.internal:6379

# Dịch vụ gửi thư giao dịch THẬT. Đây là thành phần trọng yếu ngang cơ sở dữ
# liệu: magic link là cách đăng nhập duy nhất, nên thư chết = không ai vào được.
# Xác thực tên miền gửi phải cấu hình đầy đủ, nếu không thư vào hộp thư rác và
# triệu chứng người dùng thấy là "đăng nhập không hoạt động".
KAIJU_MAIL_TRANSPORT=smtp
KAIJU_MAIL_HOST=REPLACE
KAIJU_MAIL_PORT=587
KAIJU_MAIL_USER=REPLACE
KAIJU_MAIL_PASSWORD=REPLACE
KAIJU_MAIL_FROM=no-reply@staging.kaiju.example.com

# Dịch vụ tương thích S3 THẬT.
KAIJU_OBJECT_STORAGE_ENDPOINT=https://REPLACE
KAIJU_OBJECT_STORAGE_REGION=REPLACE
KAIJU_OBJECT_STORAGE_BUCKET=kaiju-staging-attachments
KAIJU_OBJECT_STORAGE_ACCESS_KEY=REPLACE
KAIJU_OBJECT_STORAGE_SECRET_KEY=REPLACE
KAIJU_OBJECT_STORAGE_PATH_STYLE=false

KAIJU_PUBLIC_URL=https://staging.kaiju.example.com
KAIJU_TOKEN_SIGNING_KEY=REPLACE

# Đích thu thập THẬT, không phải container gói sẵn — nhà phát hành của container
# đó nói rõ nó không dành cho môi trường thật.
OTEL_EXPORTER_OTLP_ENDPOINT=https://REPLACE
OTEL_EXPORTER_OTLP_PROTOCOL=grpc
OTEL_RESOURCE_ATTRIBUTES=deployment.environment=staging

KAIJU_MIGRATE_ON_STARTUP=false
KAIJU_SEED_DATA=false
