;; Tiers 1-2 and CI only. The plaintext password is INTENTIONAL: both the
;; database cluster and this account are thrown away every time the environment
;; is rebuilt.
;;
;; Tiers 3 and 4 do NOT use this file - secrets come from the host's secret
;; storage or from the orchestrator's.
"kaiju" "kaiju"
